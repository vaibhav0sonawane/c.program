#include<stdio.h>
int main(){
    int a,b;
    printf("Enter two number:");
    scanf("%d",&a,&b);
    if(a<b)
    printf("B is greater then A");
    else
    printf("A is greater then B");
    return 0;
}